import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['s3-alpha-sig.figma.com','skillicons.dev'] // Add your domains here
  },
};

export default nextConfig;
